## What does this change?

<!-- One or two sentences on what and why. -->

## Checklist

- [ ] `xcodebuild -project MacCleanerPro.xcodeproj -scheme MacCleanerPro test` passes
- [ ] Tests added/updated for `Core/` changes
- [ ] If this touches `project.yml`, I ran `xcodegen generate` and committed the effect (not the generated `.xcodeproj` itself — that's gitignored)
- [ ] I didn't add any key material under `keys/`

## Related issue

<!-- Closes #123, or "N/A" -->
